version: '3.8'
services:
  redis:
    image: redis:7-alpine
    container_name: public-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
    name: public_redis_data
