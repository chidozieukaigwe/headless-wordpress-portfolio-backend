version: "3.8"

services:
  redis:
    image: redis:7-alpine
    container_name: chidodesigns_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data:
    name: chidodesigns_redis_data
